    <div class="container-fluid">
      <div class="forms-background-lines"></div>
      <!--Logo-->
      <div class="row mt-4 mb-3">
        <div class="col-xs mx-auto d-block">
          <%= image_tag("logo.png", :alt => "logo") %>
        </div>
      </div>


      <div class="row d-block">
        <div class="col col-sm-8 col-md-6 col-lg-6 offset-sm-2 offset-md-3 offset-lg-3">

   <!--Form-->
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :first_name,
                required: true,
                autofocus: true,
                input_html: { placeholder: "First name" }, :class => "form-control"%>
    <%= f.input :last_name,
                required: true,
                autofocus: true,
                input_html: { placeholder: "Last name" }, :class => "form-control"%>
    <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { placeholder: "Email" }, :class => "form-control"%>
    <%= f.input :password,
                required: true,
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { placeholder: "Password" }, :class => "form-control" %>
    <%= f.input :password_confirmation,
                required: true,
                input_html: { placeholder: "Confirm password" }, :class => "form-control" %>
  <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up", :class => "btn btn-lg btn-block button-sign-up" %>
  </div>
<% end %>
<<<<<<< Updated upstream
=======

<%= render "devise/shared/links" %>


<div class="container-fluid">
      <!--Logo-->
      <div class="row mt-5 mb-3">
        <div class="col-xs mx-auto d-block">
          <img class="responsive" src="./assets/img/logo.png" alt="">
        </div>
      </div>
       </div>


>>>>>>> Stashed changes
