
<div class="container">
  <div class="matches-title mt-5 mb-3">
    <h1>All Chatrooms</h1>
  </div>
  <% @chat_rooms.each do |c| %>
    <div class="matches-card">

      <div class="col-9 d-block align-self-center">
        <div class="matches-card-company col-12 text-left">
          <%= c.name%>
        </div>

        <div >
        <%= link_to 'View details', chat_room_path(c), class: "btn btn-primary" %>
        </div>

      </div>
    </div>
   <%end%>
  <div class="profile-link">
    <%= link_to offers_path, :class => "profile-link" do  %>
      <%= image_tag("rockt_button.png",  :class => "rocket-button", :alt => "footer")    %>
    <% end %>
  </div>
</div>
