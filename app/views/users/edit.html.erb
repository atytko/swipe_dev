<div class="container">
  <div class="profile-edit">
    <div class="profile-edit-title mt-5 mb-3">
      <h1>Edit Profile</h1>
    </div>
    <%= simple_form_for @user do |f| %>
      <%= f.error_notification %>
      <!-- Edit User Photo -->


      <label class="mb-4">
        <span class="fa fa-camera fa-2x"></span> Upload photo
        <span style="display:none;">
          <%= f.input :photo, class: "form-custom-label" %>
          <%= f.input :photo_cache, as: :hidden %>
        </span>
      </label>

      <%= f.input :first_name, class: "mt-3" %>
      <%= f.input :last_name, class: "mt-3"  %>

      <%= f.input :about %>
      <%= f.input :experience, class: "mt-3" %>
      <%= f.input :education, class: "mt-3" %>

      <%= f.input :location, as: :radio_buttons, collection: @location, wrapper: :vertical_collection_inline %>
      <%= f.input :skills, as: :radio_buttons, collection: @skills, wrapper: :vertical_collection_inline%>
      <%= f.input :seniority, as: :radio_buttons, collection: @seniority, wrapper: :vertical_collection_inline%>
      <%= f.input :job_type, as: :radio_buttons, collection: @job_type, wrapper: :vertical_collection_inline %>
      <%= f.input :contract_type, as: :radio_buttons, collection: @contract_type, wrapper: :vertical_collection_inline %>
      <%= f.input :position, as: :radio_buttons, collection: @position, wrapper: :vertical_collection_inline %>
      <%= f.input :benefits, as: :radio_buttons, collection: @benefits, wrapper: :vertical_collection_inline %>

      <%= f.input :min_salary, collection: @min_salary, prompt: :translate %>

      <div class="pt-4 pb-4">
        <%= f.button :submit, "Save", class: "btn btn-primary edit-submit-button" %>
        <%= button_tag t('buttons.cancel'), class: "btn btn-outline-secondary edit-cancel-button", type: "submit", name: "cancel", value: true %>
        <%= link_to "Reset", filter_path, class: "btn btn-primary edit-reset-button float-right", :method => :patch %>
      </div>
    <% end %>
  </div>
</div>

<%= link_to offers_path, :class => "profile-link" do  %>
  <%= image_tag("rockt_button.png",  :class => "rocket-button", :alt => "footer")    %>
<% end %>
